AI-log

ИИ использовал как помощника: просил набросать верстку, стили и черновик кода. Структуру проекта, нейминг, какие компоненты нужны, где живет логика и чего не делать (store, debounce, AbortController) решал сам.

UserFilters.vue
Промпт: сверстать поиск и чекбоксы полей, без лишних стилей.
Сам задал поведение: один чекбокс за раз, дефолт name. Потом попросил легкую стилизацию в том же стиле, что карточки.

Поиск, App и useSearch
Промпт: написать фильтр списка по выбранному полю по шаблону кода поиска, написанно вручную.
Логику вынес в useSearch по своему решению.

Loading и error
Промпт: показать лоадер на загрузке Users, потом обработать ошибку.
Сам выбрал vue3-loading-overlay, полноэкранный лоадер и скрытие интерфейса на время запроса. ИИ написал обвязку isLoading / error.

Декомпозиция
Промпт: разложить App.vue по выбранной схеме.
Структуру выбрал сам: useUsers, UserList, UserCard, ErrorMessage, EmptyState. ИИ только разнес код по файлам. Store не просил и не ставил.
